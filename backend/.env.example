# Backend environment (MongoDB Atlas)
# Get the password from your secrets manager — never commit it.
MONGODB_URI=mongodb+srv://waseemsamra_db_user:<db_password>@cluster0.mrxyxdi.mongodb.net/vehicle-marketplace?retryWrites=true&w=majority
PORT=5000
JWT_SECRET=replace-with-a-long-random-secret

# Seed credentials (do NOT commit real secrets)
ADMIN_USERNAME=waseemsamra@gmail.com
ADMIN_PASSWORD=change-me
